1. √ 将 MessageBufferCheckProcessor 合并到 UmaTCPHandler
2. √ 将 MessageReceiveController 合并到 AllController 或者 UmaTCPHandler
3. √ 合并其他的 controller 到 AllController
4. 去掉已经线程安全集合的锁(synchronized)
5. √ 使用接口引用 Queue
6. √ 激发器做白名单（避免大量激发器），目前先过滤掉sn 不是数字的激发器 ，在分包线程中做该处理
        后期考虑在页面配置。
7. √ 读卡器下发指令一次不一定会成功，需要多次发送。
8. √性能优化，系统启动后计算机cpu 大量消耗。愿意：规则匹配和往返规则匹配线程，大量操作数据库。
           解决办法：有新的位置信息时存放进位置信息队列，规则匹配线程判断队列是否有数据来决定是否做规则匹配。
     		用户触发新的往返规则时，将规则对象存放在规则队列，往返规则匹配线程判断规则队列是否有数据来决定是否做往返规则匹配。
9. √针对8的进一步优化，在匹配规则和往返规则时，定位到用户。
10.√配置文件添加激发器白名单
11.设备关联站点。